# We'll provide diff outputs based on age
# 1 - 18 -> Important
# 21, 50, > 65 -> Important
# All others -> not Important

# Receive age and store in age
age = eval(input("Enter age: "))

# and : If both are true it returns true
# or : If either cond is true then true
# not : Convert true cond into a false

# if age is both greater than or equal to 1 and less than or equal to 18 we want to print important
if (age >= 1) and (age <= 18):
    print("Important Birthday")

# if age is either 21 or 50 then its going to be important
elif (age == 21) or (age == 50):
    print("Important Birthday")

# we check if age is less than 65 and then convert true to false
elif not(age < 65):
    print("Important Birthday")

# Else not important
else:
    print("Sorry, Not Important")
